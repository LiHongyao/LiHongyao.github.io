export const UPDATE_TITLE = (title) => ({
    type: "UPDATE_TITLE",
    title
});

export const IS_SHOW_TABBAR = (isShowTabBar) => ({
    type: "IS_SHOW_TABBAR",
    isShowTabBar
});

export const INFOS = (infos) => ({
    type: "INFOS",
    infos
});